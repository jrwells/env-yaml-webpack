{
  "name": "env-yaml-webpack",
  "version": "0.0.1",
  "description": "A simple webpack plugin to suppot env-yaml",
  "main": "index.js",
  "scripts": {
    "build": "babel --copy-files --out-dir dist src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Robert Wells",
  "license": "ISC",
  "peerDependencies": {
    "webpack": ">=1.14.0"
  },
  "dependencies": {
    "env-yaml": "github:jrwells/env-yaml"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.2.3",
    "babel-plugin-transform-object-assign": "^6.22.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.24.1"
  },
  "babel": {
    "presets": [
      "es2015"
    ],
    "plugins": [
      "transform-object-assign"
    ]
  }
}
